// coi.js -- the front page's service worker, beside index.html so it may control it:
// a worker reaches only the pages under its own path. the work is inle/wasm/coi.js.
importScripts('inle/wasm/coi.js');
