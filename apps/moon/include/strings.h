#ifndef _AI_STRINGS_H
#define _AI_STRINGS_H
/* POSIX strings.h -- the case-blind compares, which string.h already declares.
 * the header exists because portable code includes it by name. */
#include <string.h>
#endif
