# The bare-metal RP2040 build (no CMake/SDK) writes all output under the
# top-level out/rp2040 -- nothing build-generated lands here. boot2.bin is a
# vendored source artifact (the stage-2 payload pad_checksum.g stamps), so it
# is tracked despite the .bin extension.
